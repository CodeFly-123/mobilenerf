# Vision MCP Server 最终配置总结

## 🎯 目标地址已更新
**MCP服务地址**: `http://10.33.15.3:8796/mcp`

## 📋 配置完成情况

### ✅ 已完成的配置
1. **自定义MCP服务器** - `custom-vision-mcp-server.js`
2. **API配置** - 内置到代码中，无需环境变量
3. **Dify导入配置** - 支持streamable-http方式
4. **测试脚本** - 用于验证连接

### 🔧 需要您完成的步骤

#### 1. 配置API密钥
编辑 `custom-vision-mcp-server.js` 文件，将第6行的API密钥替换：
```javascript
const CUSTOM_API_KEY = "your-actual-api-key-here";  // 替换为您的实际API密钥
```

#### 2. 启动MCP服务器
```bash
cd /workspace
node custom-vision-mcp-server.js
```

#### 3. 在Dify中导入MCP服务
使用以下地址：
```
http://10.33.15.3:8796/mcp
```

## 📁 文件说明

| 文件名 | 用途 |
|--------|------|
| `custom-vision-mcp-server.js` | 主MCP服务器文件 |
| `dify-mcp-import.json` | Dify导入配置 |
| `test-mcp-connection.js` | 连接测试脚本 |
| `Dify导入指南.md` | 详细导入说明 |

## 🚀 快速开始

### 步骤1：配置API密钥
```bash
# 编辑配置文件
nano custom-vision-mcp-server.js
# 找到第6行，替换API密钥
```

### 步骤2：启动服务
```bash
node custom-vision-mcp-server.js
```

### 步骤3：在Dify中导入
1. 打开Dify管理界面
2. 进入设置 → 模型供应商
3. 添加MCP服务
4. 输入地址：`http://10.33.15.3:8796/mcp`
5. 测试连接并保存

## 🔍 测试验证

### 测试MCP连接
```bash
node test-mcp-connection.js
```

### 测试图片分析功能
在Dify中调用 `analyze_image` 工具：
```json
{
  "name": "analyze_image",
  "arguments": {
    "image": "https://example.com/image.jpg",
    "prompt": "请描述这张图片"
  }
}
```

## 📊 服务架构

```
Dify → http://10.33.15.3:8796/mcp → custom-vision-mcp-server.js → http://10.33.15.1:8808/v1 (Qwen2.5-VL-7B-Instruct)
```

## ⚠️ 注意事项

1. **网络连接**：确保所有地址都可访问
2. **API密钥**：必须正确配置
3. **服务状态**：确保MCP服务持续运行
4. **防火墙**：确保端口8796开放

## 🎉 完成后的功能

- ✅ 支持图片内容分析
- ✅ 支持多种图片格式
- ✅ 可自定义分析提示词
- ✅ 完全集成到Dify工作流中

配置完成后，您就可以在Dify中使用强大的图片分析功能了！