# Vision MCP Server 运行说明

## 问题分析
您遇到的错误是因为在错误的目录下运行命令。错误信息显示您在 `/home/code/mcp-servers/vision-mcp/` 目录下运行，但项目文件在 `/workspace` 目录下。

## 解决方案

### 方案1：在正确目录下运行（推荐）
```bash
# 切换到项目目录
cd /workspace

# 安装依赖（如果还没安装）
npm install

# 配置API密钥
# 编辑 custom-vision-mcp-server.js 文件，将 'your-api-key-here' 替换为您的实际API密钥

# 启动服务器
node custom-vision-mcp-server.js
```

### 方案2：使用启动脚本
```bash
# 使用我们创建的启动脚本
/workspace/run-vision-mcp.sh
```

### 方案3：复制项目到您的目录
```bash
# 将项目文件复制到您的工作目录
cp -r /workspace/* /home/code/mcp-servers/vision-mcp/
cd /home/code/mcp-servers/vision-mcp/
npm install
node custom-vision-mcp-server.js
```

## 当前项目状态

### 文件位置
- 项目根目录：`/workspace`
- 主文件：`/workspace/custom-vision-mcp-server.js`
- 依赖已安装：`/workspace/node_modules/`

### 需要配置
1. **API密钥**：编辑 `custom-vision-mcp-server.js` 第6行
2. **启动服务**：在 `/workspace` 目录下运行

## 验证步骤

### 1. 检查目录
```bash
pwd  # 应该显示 /workspace
ls -la custom-vision-mcp-server.js  # 应该能看到文件
```

### 2. 检查依赖
```bash
ls -la node_modules/@modelcontextprotocol/sdk  # 应该能看到SDK目录
```

### 3. 配置API密钥
```bash
# 编辑配置文件
nano custom-vision-mcp-server.js
# 找到第6行：const CUSTOM_API_KEY = "your-api-key-here";
# 替换为：const CUSTOM_API_KEY = "your-actual-api-key";
```

### 4. 启动服务
```bash
node custom-vision-mcp-server.js
```

## 成功标志
如果配置正确，您应该看到：
```
Custom Vision MCP Server started successfully
API Base URL: http://10.33.15.1:8808/v1
Model: Qwen2.5-VL-7B-Instruct
```

## 下一步
服务启动后，您就可以在Dify中使用 `http://10.33.15.3:8796/mcp` 地址导入MCP服务了。