# Vision MCP Server 配置说明

## 重要：配置API密钥

在使用前，您需要编辑 `custom-vision-mcp-server.js` 文件，将API密钥替换为您的实际密钥：

```javascript
// 找到文件中的这一行（第6行左右）
const CUSTOM_API_KEY = "your-api-key-here";  // 替换为您的实际API密钥

// 改为：
const CUSTOM_API_KEY = "your-actual-api-key-here";
```

## 在Dify中导入MCP服务

### 方法1：直接使用HTTP地址
在Dify的MCP服务配置中，直接使用：
```
http://10.33.15.3:8796/mcp
```

### 方法2：使用配置文件
使用 `dify-import-config.json` 中的配置：

```json
{
  "mcpServers": {
    "vision-mcp-server": {
      "type": "streamable-http",
      "url": "http://10.33.15.3:8796/mcp"
    }
  }
}
```

## 启动服务器

配置好API密钥后，启动服务器：

```bash
cd /workspace
node custom-vision-mcp-server.js
```

## 测试

运行测试脚本：
```bash
node test-vision-mcp.js
```

## 注意事项

1. 确保您的Qwen2.5-VL-7B-Instruct模型服务器在 `http://10.33.15.1:8808/v1` 可访问
2. 确保MCP服务地址 `http://10.33.15.3:8796/mcp` 可访问
3. 替换API密钥后即可正常使用